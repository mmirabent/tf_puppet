#cloud-config
write_files:
- encoding: b64
  content: ${ssh_priv}
  owner: ${user}:${user}
  path: /home/${user}/id_rsa.internal
  permissions: '0600'

runcmd:
- [ yum, update, -y ]

preserve_hostname: false
hostname: ${hostname}
fqdn: ${hostname}.local
manage_etc_hosts: true